# INTEGRATION — 자극 ↔ 통합 러너 인터페이스

세탁 광고(`laundry-ad/`)와 게임(`game/`)은 **각자 독립 실행되는 자극**이고,
통합 러너(`session.html`, 예정)가 iframe으로 띄워 순서·설문·로그를 총괄한다.
자극은 참가자 ID를 묻지 않고, 설문을 띄우지 않고, 파일을 내보내지 않는다. 전부 러너 몫이다.

## 1. 러너 → 자극 (URL 파라미터)

| 파라미터 | 값 | 의미 |
|---|---|---|
| `mode` | `watch` \| `intervene` | 조건. 게임 내부 옛 이름 `fail`/`rewind`는 폐기 |
| `ver` | `A` \| `B` | 평행 자극 버전. 같은 참가자가 같은 자극을 두 번 겪으므로 소재를 교체한다 |
| `sid` | 문자열 | 참가자 세션 ID. 러너가 발급하고 4블록 전체에 같은 값을 넘긴다 |
| `block` | `1`~`4` | 이 자극이 몇 번째 블록인지 (순서 효과 분석용) |
| `debug` | `1` | 개발용 패널 |
| `sound` | `0` | 효과음을 끈다(파일럿·심사용). 기본값은 **켜짐**이고 두 자극이 같다 |

게임 전용:

| 파라미터 | 값 | 의미 |
|---|---|---|
| `mode=tutorial` | — | 게임 자체의 조작 연습. **러너는 더 이상 띄우지 않는다**(아래 §5-5). 단독 실행·점검용으로 남아 있다 |

두 자극 모두 **iframe 이 뜨는 즉시 재생을 시작한다.** 자극 안에 시작 버튼도, 조건 안내 문구도
없다. "이번에는 보기만 하세요 / 직접 조작하세요" 같은 안내는 러너가 블록 시작 화면에서
두 자극에 **같은 형식으로** 제시해야 한다. 자극마다 안내 방식이 다르면 그 자체가 변량이 된다.

## 2. 자극 → 러너 (종료 통지)

종료 시점에 **세 곳 모두**에 같은 payload를 기록한다.

```js
window.AD_RESULT = payload;                                  // 디버깅·테스트 하네스용
window.parent.postMessage({ type: 'AD_DONE', payload }, '*'); // 러너가 받는 정식 경로
localStorage['ad_log_' + sid + '_' + stim + '_' + mode] = JSON.stringify(payload);  // 백업
```

`postMessage`가 유일한 정식 경로다. localStorage는 러너가 죽었을 때를 위한 백업이며,
키에 `stim`·`mode`가 들어가는 이유는 한 참가자(`sid` 하나)가 4블록을 돌기 때문이다.

> **미해결:** `laundry-ad/scenes.js`는 아직 `'ad_log_' + sid` 만 쓴다(블록 간 덮어씀).
> 러너 작업 때 위 규격으로 맞춘다.

## 3. 공통 로그 필드

두 자극이 같은 이름·같은 의미로 기록한다. 해당 없는 항목은 `null`(공란)이지 `0`이 아니다.

| 필드 | 세탁 | 게임 |
|---|---|---|
| `sid` `mode` `ver` `block` | 러너가 준 값 그대로 | 〃 |
| `stim` | `'laundry'` | `'game'` (튜토리얼은 `'game_tutorial'`) |
| `t_start` `t_end` | epoch ms | 〃 |
| `DWELL_TOTAL` | 초, 자극 시작~종료 | 〃 |
| `DWELL_INT` | 장면 6 체류 | 되감기 후 구출 구간 체류 |
| `INT_DONE` | 시트 투입 성공 1/0 | 구출 성공 1/0 |
| `INT_ATTEMPTS` | 목표 밖 드롭 횟수 | 게임은 재시도가 없어 항상 0 |
| `T_FIRST_DRAG` | 장면 6 진입~첫 드래그 | 구출 구간 진입~첫 조작 |
| `T_MANIP` | 첫 드래그~성공 | 첫 조작~성공(실패 시 구간 종료까지) |
| `T_REWIND` | 장면 5의 [되돌리기]를 누르기까지 | 실패 화면의 [되돌리기]를 누르기까지 |
| `HINT_SHOWN` | 힌트 노출 횟수 | 게임은 힌트가 없어 항상 0 |
| `CTA_CLICK` | 구매 버튼 클릭 1/0 | 다운로드 버튼 클릭 1/0 |
| `REDUCED_MOTION` | OS 설정 기록 전용 | 〃 |
| `AUDIO_OK` | 소리가 실제로 났는지 1/0. 껐거나(`sound=0`) 장치가 없으면 `null` | 〃 |
| `SFX_COUNT` | 낸 신호 수(들렸는지와 무관) | 〃 |

`watch` 조건에서는 `DWELL_INT` `INT_DONE` `T_FIRST_DRAG` `T_MANIP` `T_REWIND`가 전부 `null`이다.

게임 전용 추가 필드: `RESULT`(`scripted_fail`\|`rescue_success`\|`rescue_fail`),
`COLLISION_T`(초, 시작~자동재생 충돌), `POWER_END`, `GATES_PASSED`, `VER_FALLBACK`.

## 4. 자극 길이

두 자극의 노출 시간은 **맞춰져 있다.** 한때 세탁 쪽을 나래이션 길이에 맞춰 자르면서
1.5~1.7배까지 벌어졌었고(§5-14), 양쪽을 서로에게 맞춰 되돌렸다.

| | 세탁 | 게임 | 배수 | 측정 |
|---|---|---|---|---|
| watch | 28.0초 고정 | 28.0초 (A·B) | 1.00 | `laundry-ad: npm run timing` / `game: npm test ver=A|B` |
| intervene | **39.15초 고정** + 되돌리기 대기 + 되감기 6초 + 장면 6 조작 시간 | **47.7초 고정** + 되돌리기 대기 | 1.00 (실측 49.7 : 49.7) | 〃 |
| 제품 메시지 | 8초 (장면 10) | 8초 (제품 카드) | 1.00 | 〃 |

**어느 쪽도 혼자 움직여서는 맞출 수 없었다.** 게임은 실패 각본(자동 조종이 맞게 고르다
오판한다)과 되감기 깊이가 길이의 하한을 정하고, 세탁은 나래이션 클립이 하한을 정한다.
그래서 양쪽에서 좁혔다 — 게임 −3.2초(watch) · −5.7초(intervene), 세탁 +9.4초 · +12.8초다.
자세한 것과 각각의 대가는 §5-14 에 있다.

**개입 조건의 길이는 이제 참가자마다 다르다.** 실패 뒤 되감기가 저절로 일어나지 않고
참가자가 [되돌리기]를 눌러야 시작하기 때문이다(§11). 대기 시간은 `T_REWIND` 로 따로 남으므로
분석에서 노출 시간을 쓸 때 `DWELL_TOTAL` 을 그대로 쓰지 말고 이 값을 분리할 것.
실측 예: 세탁 49.7초(대기 1.2초 + 조작 3.2초) · 게임 49.7초(대기 1.0초).

**두 자극 모두 8초짜리 제품 메시지가 이 시간 안에 들어 있다.** 세탁은 장면 10, 게임은
제품 카드다. 한때 세탁 쪽이 나래이션 길이에 맞춰 3.07초로 잘렸다가 되돌렸다 —
`CTA_CLICK` 이 종속변인이라 노출 시간이 다르면 클릭률 차이에 "누를 시간이 얼마나
있었나"가 섞인다(§5-14).
게임 타임라인의 도입부는 **두 번에 걸쳐** 걷어냈고, 두 번 다 뒤 전개를 그대로 두는
방식으로 했다. 카드를 처음 붙였을 때 기존 재생 **뒤에** 얹혀 39.2초/63.3초가 됐던 것을
앞쪽 8초(자동 조종이 전부 맞게 고르던 6개 사건)를 들어내고 시작 파워를 그 구간
통과값(23)으로 올려 되돌린 것이 첫 번째다. 노출 시간을 세탁과 맞추며 2개(옛 0.5초
게이트 +7 · 옛 1.8초 적 −7)를 더 들어낸 것이 두 번째다 — 이 짝은 **순증이 0** 이라
시작 파워가 그대로다. 순증이 0 이 아닌 짝을 고르면 `START_POWER` 를 같이 고쳐야 하고,
잘못 세면 실패 지점이 통째로 앞당겨진다(실제로 그런 적이 있다).

**두 번 다 그 뒤 전개는 수학적으로 같다** — 실패 지점의 파워도 `POWER_END = 7` 로
예전과 같고, 구출 난이도도 아래 §5-7 표 그대로다. `GATES_PASSED` 만 게이트가 줄어
watch 기준 12 → 9 → **8** 로 바뀌었다. 실패 지점은 18.2초 → **15.6초**(생성 시각)다.

게임은 원래 프레임 수로 시간을 세고 있었다(rAF 한 프레임 = 1틱). 120Hz 화면에서는 자극이
2배 빨리 끝난다는 뜻이라, 실제 경과 시간 기준 고정 간격(60틱/초)으로 바꿨다. 이제 참가자
기기의 주사율과 무관하게 같은 길이로 재생된다.

## 5. 아직 정리되지 않은 자극 간 비대칭

분석·해석에서 반드시 감안해야 하고, 가능하면 설계 단계에서 없애야 하는 것들이다.

1. **조건 내 노출 비대칭(두 자극 공통·의도된 것).** `watch`는 실패까지만 보고 끝나고,
   `intervene`만 되감기·개입·개선 결과를 본다. 노출 시간과 정보량이 조건 간에 다르다.
2. ~~게임에 평행 자극(ver B)이 없다~~ **해소됨.** `MAIN_TIMELINE_B`를 만들었다. 사건 수(게이트 12·
   적 9)·각 사건의 시각·이득/손해 값·적 HP·자동 조종의 오판 지점·실패 지점(15.6초)이 A와 같고,
   **이득 상자의 좌우 패턴과 좁은 적의 위치만 다르다.** 정확한 거울상이면 "아까 그거 뒤집은 것"으로
   읽히므로 12개 중 3개는 A와 같은 쪽에 뒀다. `npm run parallel`이 두 타임라인을 직접 비교해
   이 조건을 검사한다. `VER_FALLBACK`은 스키마에 남겨 두지만 이제 늘 0이다.
3. **소리 — 규격은 맞췄고 밀도는 아직 다르다.** 예전에는 게임의 효과음을 걷어내
   두 자극 모두 무음으로 뒀었다. 지금은 **둘 다 소리가 난다**(Web Audio 합성, 오디오 파일 없음).
   비대칭을 막기 위해 다음을 코드로 강제한다.

   | 맞춘 것 | 어떻게 |
   |---|---|
   | 신호 목록 | `start · beat · bad · fail · rewind · grab · miss · success · card · cta` 열 가지를 두 자극이 **같은 서사 자리**에서 낸다 |
   | 신호의 길이·세기 | `sfx.js` 의 `CUES` 표 하나가 정한다. 두 파일의 `SFX-CORE` 구간이 **글자 하나까지 같아야** 하고, `laundry-ad: npm test sfx` 가 직접 비교한다 |
   | 마스터 음량·리미터 | 코어의 `MASTER = 0.22` + 같은 컴프레서 설정 |
   | CTA 클릭음 | **음색까지 같다**(코어의 `CTA_VOICE`). 이 클릭이 곧 종속변인이라, 누르는 순간의 청각 보상이 다르면 그 차이가 클릭률로 들어온다 |
   | 기본값 | 둘 다 켜짐. `sound=0` 으로 둘 다 같은 방식으로 끈다 |

   **다른 것은 음색뿐이다**(`VOICES`) — 세탁은 둥근 파형, 게임은 각진 파형. 광고하는 물건이
   다르니 소리도 다르되, 다른 것이 음색 하나로 제한된다.

   **남은 비대칭: 청각 밀도.** 세탁은 장면이 바뀔 때 울리고 게임은 게이트·적을 지날 때마다
   울린다. 실측(`SFX_COUNT`):

   | | 세탁 | 게임 |
   |---|---|---|
   | watch | 5회 / 28.0초 (0.18회/초) | 19회 / 28.0초 (0.68회/초) |
   | intervene | 11회 + 스냅백 수 / 49.7초 (0.22회/초) | 38회 / 49.7초 (0.76회/초) |

   **노출 시간을 맞추면서 이 차이는 오히려 벌어졌다(2.5배 → 3.5배).** 세탁은 신호 수가
   장면 수로 정해져 있어 길이를 늘려도 그대로인데 시간만 늘었고, 게임은 사건 2개가
   줄어 신호가 21 → 19 · 42 → 38 로 줄었다. 길이를 맞춘 대가가 여기로 옮겨 온 셈이다.
   여전히 게임이 자주 울린다. 이건 두 광고 형식의 실제 차이(연속 조작 vs 장면 전환)라
   억지로 맞추면 자극이 부자연스러워지므로 **맞추지 않고 재기로 했다** — 모든 참가자의
   `SFX_COUNT` 가 로그에 남으므로 분석에서 공변량으로 넣거나 조건 간 차이를 확인할 수 있다.

   **배경음(BGM) — 자극마다 다르게 두기로 했다. 이건 감수한 교란이다.**
   세탁은 방 공기 + 장조 패드, 게임은 가단조 루프(베이스·아르페지오·선율, 100bpm)다.

   게임 쪽은 세 번 고쳤고, 세 번째에야 음악이 됐다.
   ① 55·82Hz 저역으로만 짜서 노트북·휴대폰 스피커가 통째로 잘라 냈다 — **게임 쪽만 무음**
   이 되는 비대칭이라 음역을 올렸고, `npm test sfx` 가 "전부 저역"을 잡도록 검사를 넣었다.
   ② 음역을 올려도 여전히 물속 환경음이었다. 넓은 잡음 위를 느리게 훑는 필터 스윕이
   '헤엄쳐 다니는 소리'를 만들고 있었다.
   ③ 스윕을 걷어내고 음정을 바꿔도 **거의 달라지지 않았다.** 원인은 음색이 아니라 구조였다 —
   배경음 엔진이 지속음(패드·드론)만 낼 수 있어서, 음정을 뭘로 바꾸든 웅웅거림이었다.
   그래서 코어에 **반복 악절 스케줄러**(`seqNote` · `BED.lookahead`)를 넣었다. 오디오 시계에
   미리 예약하는 방식이라 기기가 느려도 박자가 안 흔들린다 — `setTimeout` 으로 그때그때
   내면 참가자마다 다른 박으로 들린다. 이제 게임 배경음은 Am → F → C → G 를 9.6초마다
   반복하는 실제 곡이다. 이 주기는 자극 길이(28.0초 · 49.7초)와도 게이트 간격(약 2.6초)과도
   일부러 안 맞물린다 — 맞물리면 곡의 마디가 다음 사건을 예고한다.

   ④ 게임만 곡이 되자 두 배경음의 **무게가 벌어졌다.** 세탁은 가만히 깔린 패드인데 게임은
   박자와 선율이 있는 곡이라, 음량 천장이 같아도 존재감이 달랐다(200Hz 이상 세기 0.16 : 0.55).
   그래서 **세탁에도 같은 스케줄러로 악절을 얹었다.**

   | | 세탁 | 게임 |
   |---|---|---|
   | 화음 | C → Am → F → G | Am → F → C → G |
   | 빠르기 · 한 바퀴 | 100bpm · 9.6초 | 100bpm · 9.6초 |
   | 가장 잔 음표 | 4분음표(0.6초) | 8분음표(0.3초) |
   | 파형 | 사인 · 삼각 | 삼각 · **사각** |
   | 라우드니스(K-가중) | −53.5 LUFS | −53.4 LUFS |
   | 밝기(스펙트럼 중심) | 295Hz | 225Hz |
   | 레벨 맞춤 계수 `bedLevel` | 1.68 | 0.60 |

   ⑤ 그래도 세탁 쪽이 **몰입되지 않는다**는 판단으로 한 번 더 고쳤다. 원인은 화음이 아니라
   **패드가 화음을 안 따라간 것**이었다 — C3·G3·C4 에 고정된 지속음이라 F·G 마디에서도 C 가
   울렸고, G 위에서 sus4 로 매달린 채 풀리지 않았다. 진행이 있는데 한 덩어리로 들린 이유다.
   여기에 방 공기(0.34)·베이스(0.36)·패드 C3(0.28) 이 130Hz 부근에 겹쳐 화음 자체가 묻혔다.
   고친 것: **패드를 마디마다 음이 바뀌는 두 성부로** 바꾸고(dur > step 으로 겹쳐 끊기지 않게),
   저역 세 겹을 걷어내고(방 공기 0.34 → 0.16, 130Hz 지속음 제거), 선율을 마디당 한 음에서
   실제 가락(C5 D5 | A4 | F4 G4 | G4)으로 바꿨다. **지속음을 다시 넣으면 같은 문제가 돌아온다.**
   200Hz 이상 세기는 0.48 → 0.68 이 됐다.

   ⑥ **두 배경음이 9dB 차이 나고 있었다 — 아무 검사도 안 걸렸다.**

   여기까지의 자는 세기표(`sfx.test.js` 의 `audibleGain`)였다. 소스에서 `gain` 값을 긁어
   200Hz 위 레이어의 세기를 더하는 것이라 **음표 밀도·쉼표·감쇠·배음·지속음 여부를 못 본다.**
   그래서 "세탁 0.68 > 게임 0.55" 로 세탁이 더 세다고 읽고 있었는데, 실제 신호를 만들어
   재 보니 **게임이 9.0dB 컸다**(K-가중 −49.0 vs −58.0 LUFS). 10dB 가 "두 배로 크게"다.

   원인은 지속음이다. 게임의 사인 패드 두 층(110Hz · 164.81Hz)만 빼면 −49.0 → −54.4 LUFS 로
   떨어진다 — **패드 둘이 나머지 전부를 합친 것보다 크다.** 패드는 duty 100% 인데 세탁의
   악절들은 지수 감쇠라 duty 가 28~71% 다. 그리고 세탁에는 대응할 지속음이 없다. ⑤에서
   고정 드론을 걷어냈기 때문이고, 그 판단 자체는 옳았다(다시 넣으면 sus4 문제가 돌아온다).
   즉 **한쪽만 드론을 쓸 수 있는 구조**였고 세기표는 그것을 볼 수 없었다.

   고친 것: 층 세기를 일곱 개씩 흩어 고치면 음색 문서와 어긋나므로, 레벨은 **파일마다 한 줄**
   (`createSfx` 의 `bedLevel`)로 두고 코어가 `BED.gain` 에 곱한다. 세탁 ×1.68 · 게임 ×0.60 →
   둘 다 −53.5 LUFS 다. 균등 배율이라 각 곡의 층 간 균형(=음색)은 그대로다.

   **큐 헤드룸도 같이 확인했다.** 세탁을 1.68배 올리면 배경음 최대치가 가장 작은
   큐(`beat`)와 4.9dB 까지 붙는다(올리기 전 9.4dB). 다만 그 최대치는 마림바 어택 한 점이고,
   큐가 실제로 차지하는 시간 규모(100ms 창)로 보면 세탁 22.3dB · 게임 24.2dB 아래다.
   오히려 **두 자극의 큐-배경음 관계가 서로 가까워졌다.**

   재는 도구는 `laundry-ad/tools/bed-render.js` 다(`npm run bed`). 코어의 `buildBed`·`seqNote`
   를 그대로 다시 해서 파형을 만든다 — 대역제한 파형·같은 LCG 잡음·Cookbook biquad 를 쓰고,
   손계산과 0.3dB 안에서 일치한다. `npm test sfx` 가 이 도구로 **라우드니스 차이 1.5dB 이내**와
   **큐 대비 15dB 이상 여유**를 검사한다. 세기표만 보던 자리는 이걸로 갈았다.

   **네 화음도 빠르기도 한 바퀴 길이도 이제 라우드니스도 같다.** 남긴 차이는
   **조성(장/단)과 음표의 성김·파형**, 그리고 그에 딸린 밝기(295 : 225Hz)와 출렁임
   (22.4 : 5.2dB)이다 — 광고 성격의 차이라 맞추지 않는다.

   **그래도 마지막 판단은 귀다.** `npm run bed -- --wav` 가 두 배경음을 WAV 로 뽑는다
   (참가자가 듣는 watch 28초 · 효과음 없이 배경음만 · 두 파일에 같은 이득 +30dB).
   같은 기기·같은 음량으로 번갈아 들어 보고, 파일럿에서 광고 태도(블록 C)를 볼 때 함께 볼 것.

   세탁의 9.6초(마디 2.4·4.8·7.2·9.6초)는 장면 경계와 겹치지 않는다. 겹치면 곡의 마디가
   장면 전환을 예고한다 — 길이를 고칠 때 두 자극 모두 이걸 먼저 확인할 것.
   노출 시간을 맞춘 뒤 다시 쟀다: watch 경계는 4.00·6.60·12.60·20.00초이고, 한 바퀴
   안으로 접으면 4.00·6.60·3.00·0.80 — 가장 가까운 마디와도 **0.60초** 떨어져 있다
   (예전 0.28초보다 여유가 늘었다). `scenes.js` 의 `DUR` 표를 고치면 이 계산을 다시 할 것.

   **곡으로 인지되는 정도도 이때 같이 회복됐다.** 세탁 watch 가 18.6초일 때는 한 바퀴를
   겨우 넘겨 진행이 한 번밖에 안 들렸는데(게임 31초·세 바퀴), 지금은 28.0초로 2.9바퀴다.
   개입형은 49.7초로 5.2바퀴이고 게임도 같다.

   | 맞춘 것 | 어떻게 |
   |---|---|
   | 음량 천장 | 코어의 `BED.gain = 0.075` × 파일마다의 `bedLevel`(세탁 1.68 · 게임 0.60) |
   | 실제 크기 | 둘 다 −53.5 LUFS. 천장이 아니라 **잰 값**으로 맞춘다 — 위 ⑥ 참고 |
   | 페이드 | 인 1.6초 · 아웃 0.9초. 코어가 정한다 |
   | 켜고 끄는 시점 | 자극 시작(세탁 `Engine.start` · 게임 마운트)에 켜고 종료에 끈다 |
   | 잠금 처리 | 큐와 같은 경로. 잠금이 늦게 풀리면 그 순간 자동으로 켜진다 |
   | 끄는 법 | `sound=0` 이면 큐와 함께 꺼진다 |

   다른 것은 음색(`BED_VOICE`)뿐이고, `npm test sfx` 가 위 항목이 코어에 남아 있는지와
   음색이 실제로 다른지를 둘 다 검사한다.

   **대가.** 배경음은 0.1~0.9초짜리 큐와 달리 자극이 끝날 때까지(세탁 11~27초 · 게임 31~55초)
   내내 깔리는 지배적 자극이다.
   블록 B(짜증)·C(광고 태도)·D(제품 태도)에서 자극 간 차이가 나올 때 그게 **광고 형식**
   때문인지 **음악** 때문인지 이 설계로는 갈리지 않는다. 음색을 맞추자는 대안(두 자극이
   같은 앰비언트 베드)이 있었고, 광고로서의 자연스러움을 택해 자극별로 가기로 정했다.
   따라서 **분석에서 자극 간 B·C·D 차이를 형식의 효과로 단정하면 안 된다.**
   파일럿에서 두 광고의 기저 차이를 반드시 확인하고, 필요하면 무음 조건(`sound=0`)
   비교군을 두는 것을 검토할 것. `SFX_COUNT` 는 큐만 세므로 배경음은 이 값에 안 잡힌다.
4. **시작 제스처 — 소리 때문에 다시 문제가 된다.** 브라우저는 사용자 제스처 없이 소리를
   내지 못하게 막는데, 두 자극 모두 시작 버튼이 없고(§1) 특히 **시청 조건 참가자는 광고를
   보는 동안 화면을 한 번도 건드리지 않을 수 있다.** 그러면 그 참가자만 무음으로 본다.

   - 러너가 대신 푼다: 참가자가 러너 화면에서 버튼을 누르는 순간(제스처를 들고 있는 순간)
     같은 출처 iframe 의 `AD_SFX.unlock()` 을 직접 부른다. 자극은 뜨자마자 `AD_READY` 를
     보내 한 번 더 시도하게 만든다(`preview.html` 의 `unlockFrameAudio`).
   - 그래도 막힐 수 있으므로 **실제로 소리가 났는지를 `AUDIO_OK` 로 참가자마다 기록한다.**
     이 값이 없으면 "소리를 통제했다"고 말할 수 없다. 파일럿에서 `AUDIO_OK = 0` 이
     나오는 브라우저가 있는지 반드시 확인할 것 — 자극은 여전히 즉시 재생을 시작한다.
5. ~~튜토리얼이 게임에만 있다~~ **해소됨.** 연습을 광고 밖으로 꺼내 `practice.js`(러너 화면)로
   옮겼다. 사전 문항 직후 **한 번**, 모든 참가자에게 **같은 자리**에서 두 조작(도형 드래그 ·
   좌우 이동)을 함께 익힌다. 4블록 순서를 섞는 설계라 연습이 특정 광고에 붙어 있으면
   ① 연습 위치가 참가자마다 달라져 순서효과와 뒤엉키고 ② "연습 다음은 그 광고"라는 단서가
   생기고 ③ 그 자극만 사전 노출을 더 받는다 — 세 가지가 한꺼번에 사라진다.
   연습에 **도형만 쓰는 이유**는 게임 화면으로 연습시키면 게임 세계를 광고보다 먼저 보게 되고,
   시트로 연습시키면 "제품을 안 써서 실패한다"는 세탁 광고의 전제가 미리 새기 때문이다.
   게임 자극의 `mode=tutorial` 은 코드에 남아 있으나 러너는 쓰지 않는다.
6. **개입형 길이가 결과에 따라 달라진다(게임만).** 구출에 성공하면 55초지만 도중에 실패하면
   그 시점에 끝나 40초 안팎으로 짧아진다. 세탁 쪽 개입형은 성공 여부와 무관하게 55초다.
   즉 게임 개입 조건에서는 **노출 시간이 참가자의 수행과 엮여 있다** — 실패한 참가자일수록
   덜 노출된다. 분석에서 `DWELL_TOTAL`을 그대로 비교하면 안 되고, `RESULT`(rescue_success /
   rescue_fail)를 함께 봐야 한다.
7. **구출 난이도.** `npm run difficulty ver=A|B` 로 조작 정책별 성공률을 잰다. 3회 측정값:

   | 조작 정책 | ver A | ver B |
   |---|---|---|
   | 주의(100% 정답) | 100% | 100% |
   | 60% 정답 | 33% | 67% |
   | 30% 정답 | 0% | 0% |
   | 무조작 | 0% | 0% |

   (노출 시간을 맞추며 도입부 2개를 더 걷어내고 구출 길이를 20.3 → 18.0초로 줄인 뒤
   다시 잰 값이다. 되감아 돌아가는 자리의 파워를 23 으로 맞춰 두었으므로 — `REWIND_FLOOR_SEC`
   주석 참고 — 참가자가 겪는 구간 자체는 걷어내기 전과 같다. 양 끝은 그대로고 A 의 60% 정책만
   67% → 33% 로 움직였는데, 아래에 적은 대로 이 값은 3회 측정에서 원래 흔들리는 자리다.)

   양 끝(주의·무조작)은 A·B가 같고 중간대만 갈린다. **3회 측정이라 1판 차이가 33%p로 나타나는
   폭이고, 60%·30% 정책은 `Math.random()` 으로 고르므로 측정할 때마다 값이 흔들린다** —
   실제로 같은 코드로 잰 A의 60% 정책이 50%·33%·67% 사이를 오간 기록이 있다. 판정 구간
   (30~90%) 안이지만 **B가 중간대에서 조금 더 후할 가능성**은 남아 있고, 3회로는 가릴 수 없다.
   합성 조작이라 실제 참가자 성공률은 브라우저에서 사람이 직접 해 봐야 확정된다.
8. ~~두 자극의 표시 크기가 다르다~~ **해소됨.** 게임 캔버스가 480×720 픽셀 고정이고 CSS가
   `max-width:100%`뿐이라 **줄어들기만 하고 커지지 않았다.** 러너의 자극 틀(9:16)보다 화면이
   넓으면 게임만 원래 크기로 한가운데 박혀 있었고, 세탁 자극은 늘 틀을 꽉 채웠다. 화면이
   클수록 격차가 벌어져 틀 넓이의 57%까지 내려갔다 — 참가자 기기마다 다르게 어긋나는 차이였다.
   캔버스를 늘어나게 고치고, 좌표계를 480×853(9:16)으로 바꿔 세탁 자극과 같은 비율로 맞췄다.
   **게임 진행은 그대로다** — 배의 y는 550 고정이고 게이트·적은 y=-120에서 생겨 배까지 늘
   670px를 이동한다. 사건 시각도 반응 시간도 안 바뀌고, 늘어난 133px는 전부 배 아래쪽
   빈 우주다. 실측으로 확인했다(당시 watch 31.2초 · intervene 55.3초 · 난이도 표 그대로.
   지금 값은 §4 참고 — 제품 카드가 들어오며 도입부를 걷어냈지만 총 길이는 같다).
9. ~~게임 광고만 "광고답지 않다"~~ **해소됨.** 게임 쪽은 삼각형 우주선이 `+8`·`HP 24` 라고
   적힌 상자 사이를 지나는 화면이었다. 세탁 쪽은 사람이 나오고 빨래가 망하고 표정이 바뀌는
   이야기인데, 게임 쪽만 계산 화면이면 블록 C·D(광고 태도·제품 평가)의 차이가 **광고 형식**
   때문인지 **완성도** 때문인지 갈리지 않는다. 게임 화면을 심해로 다시 그렸다 — 잠수정이
   소나 빔을 쏘며 내려가고 물고기 떼가 헤엄쳐 올라온다. 떼가 갈라져 생긴 두 갈래 물길 중
   하나를 고르며, 맑은 쪽과 물고기가 들어찬 쪽이 색으로 구분돼 숫자를 읽지 않아도 어느 쪽이
   손해인지 보인다. 큰 떼 앞에는 붉은 눈의 포식자가 앞장서고, 못 이길 떼가 다가오면 빔이
   붉어져 실패를 미리 알린다.

   **빔은 잠수정이 알아서 쏘지만, 겨냥은 참가자 몫이다** (아래 §10 에서 바뀐 부분이다).
   발사 버튼은 없다 — 조작은 좌우 이동 하나 그대로이고, 참가자는 기뢰 앞으로 옮겨서 맞힌다.

   **바꾼 것은 그림뿐이다.** 타임라인·파워·판정·노출 시간·난이도·A/B 평행성 전부 그대로고,
   실측값(`watch 31.2초` · `intervene 55.4초` · `POWER_END 7` · `COLLISION_T 20.6초`)이
   바꾸기 전과 같다. (그때의 값이다 — 지금 길이는 §4 참고.)

   **곤충을 쓰지 않은 이유**: 벌레 혐오·밀집공포는 개인차가 크고, 세탁 광고에는 혐오 자극이
   전혀 없다. 게임 광고에만 있으면 광고 태도 차이가 형식이 아니라 징그러움에서 올 수 있다.

   **그 뒤 두 번 더 고쳤다 — 상자를 완전히 지웠다.** 심해로 다시 그린 뒤에도 물길과 적을
   둥근 사각형으로 그리고 그 안에 물고기를 넣고 있었다. 눈에 먼저 들어오는 것이 상자라
   "박스가 내려온다"로 읽혔고 게임을 하는 감각이 안 났다. 테두리를 지우고 물빛 띠만 남겨
   봤지만 여전히 띠가 내려오는 화면이었다. 그래서 **물길에는 아무것도 안 그리기로 했다.**

   게이트 하나에는 늘 이득 쪽 하나와 손해 쪽 하나가 있다(두 타임라인 전부). 그래서 손해
   쪽만 떼가 메우고 이득 쪽은 빈 물로 두면, 화면이 "떼가 반쪽을 막고 내려온다 → 빈 쪽으로
   피한다"가 된다. 적의 몸도 무리 자체이고, 숫자는 판 없이 글자만 띄운다.

   그리고 게이트의 벽이 20px 짜리 얇은 띠라 물고기가 화면에 거의 안 보였으므로, 판정과
   무관한 **배경 떼**를 화면 전체에 띄웠다 — 색을 판정용 무리(보라)와 다른 푸른색으로 두어
   색 신호는 흐려지지 않는다. 배치는 seed 로 고정한다(참가자마다 다른 화면이면 안 된다).

   마지막으로 **흐름을 끊기지 않게** 했다. 떼를 판정 구간에 딱 맞춰 그리면 사각형이 하나씩
   내려오는 화면이 된다. 떼의 위아래 가장자리를 흐리게 해 구간 밖으로 넘겨 그렸고(가운데는
   그대로 진하다), 배경 떼를 78마리로 늘려 위에서부터 끊임없이 흐르게 했다. 잠수정의 빔은
   지나는 자리의 **배경 떼만** 옆으로 가른다 — 판정용 무리를 밀면 "쏴서 없앴다"가 되어
   실패의 원인이 선택에서 조작 능력으로 옮겨 간다.

   **판정은 한 글자도 안 건드렸다.** 떼가 판정 구간(40px)을 정확히 채우고, 이득 쪽에 그림이
   없어도 참가자가 하는 일은 좌우를 고르는 것뿐이라 세로 위치는 조작에 안 들어간다.
   실측도 그대로다(`31.2초` · `55.4초` · `POWER_END 7` · `COLLISION_T 20.6초` — 그때의 값이다).

   **남은 것**: 두 광고의 "완성도"가 이제 비슷한지는 결국 사람이 봐야 판단된다. 파일럿에서
   광고 태도(블록 C)의 자극 간 기저 차이를 확인할 것.

10. **게임 자극에 반사신경 요소를 넣었다 — 자극의 전제가 바뀌었다. 감수하고 넣은 것이다.**

    게임이 "고르기만 하는 화면"이라 재미가 없다는 판단으로, **기뢰 요격**을 넣었다.
    기뢰가 안전한 물길의 **반대쪽**에서 내려오고, 잠수정 앞으로 옮겨 빔에 맞히지 못하면
    파워가 7 깎인다. 발사 버튼은 만들지 않았다 — 조작은 좌우 이동 하나 그대로다.
    난이도는 **손이 느려서**가 아니라 **게이트를 고르는 동시에 겨냥해야 해서** 올라간다.

    **깨진 것.** 이 자극의 원래 전제는 "실패는 자동 조종의 **선택** 때문이지 조작 능력
    때문이 아니다" 였다(§5-1 · SPEC 4장의 1단계 조작 요건). 이제 개입 조건의 성공/실패에
    조준 능력이 섞인다. 따라서:

    - **유능감·통제감(블록 A) 문항**을 "개입 설계의 효과"로 읽을 수 없다. 게임 쪽 점수에는
      조작 능력이 들어간다.
    - **세탁(시트 한 장 드래그, 1단계)과 조작량이 더 벌어졌다.** 두 자극의 조작 난이도 차이가
      광고 형식의 효과와 섞인다.
    - `INT_DONE`(개입 성공)의 뜻이 두 자극에서 달라졌다. 세탁은 "했는가", 게임은 "해냈는가"다.

    **지킨 것.** 시청 조건은 한 값도 안 달라졌다 — 자동 조종이 위치와 무관하게 늘 요격에
    성공하기 때문이다(광고 속 자동 조종의 실패는 선택 때문이어야 한다). 실측으로 확인했다:
    `31.2초` · `POWER_END 7` · `GATES_PASSED 9` · `COLLISION_T 20.6초`(그때의 값이다 — §4).
    개입 조건 길이(55.4초),
    난이도 밴드(주의 100% · 대충 33% · 무조작 0%), A·B 평행성도 그대로다. 기뢰는 A·B 에서
    같은 시각에 같은 수로 나오고, 두 판 모두 안전한 물길의 반대쪽에 놓인다(`npm run parallel`).

    **처음 나오는 기뢰에만 한 줄 안내를 띄운다.** 배우지 않은 규칙 때문에 실패하면 그 실패는
    '선택'이 아니라 '몰라서'가 된다. 조작할 수 없는 **시청 조건에는 안 띄운다** — 세탁 자극이
    드롭존 안내를 intervene 에만 띄우는 것과 같은 규칙이다.

    **확인이 필요하다.** 이건 그림 수정이 아니라 자극의 전제 변경이다. 지도교수 확인을 받고,
    받은 결과에 따라 이 항목과 SPEC 4장(1단계 조작 요건)을 정리할 것. 되돌리려면 두 타임라인의
    `mine(...)` 네 줄씩을 지우면 된다 — 나머지 코드는 기뢰가 없으면 아무 일도 하지 않는다.

11. **되감기를 참가자가 시작하게 바꿨다 (두 자극 모두).**

    예전에는 실패를 보여 준 뒤 광고가 **알아서** 되감았다(세탁 6초 · 게임 1초). 그러면
    되돌리는 주체가 광고이고 참가자는 구경꾼이다. 이 연구가 재려는 것이 "내가 고쳤다"는
    경험이므로, **고치기로 하는 첫 결정도 참가자가 내려야 한다.** 지금은 실패 화면이 멈춘 채
    [되돌리기] 버튼이 뜨고, 누르면 그때 역재생이 시작된다.

    | | 세탁 | 게임 |
    |---|---|---|
    | 어디 | 장면 5 | `rewind_watch` 단계 |
    | 대기 | 무한정(장면 6과 같은 규칙) | 무한정 |
    | 누른 뒤 | 역재생 6초 → 장면 6 | 역재생 → 구출 구간 |
    | 안내 | 버튼이 계속 맥동한다. 대신 눌러 주지는 않는다 | 〃 |
    | 기록 | `T_REWIND` | `T_REWIND` |

    **두 자극에 같이 넣었다.** 한쪽만 참가자가 누르게 하면 "이 광고에서 내가 주도했는가"가
    자극마다 달라져, 조작점검(섹션 F)·통제감(블록 A)의 자극 간 차이에 그대로 얹힌다.
    버튼의 크기·자리·눌림 반응은 두 자극이 같은 cq 값을 쓴다.

    **대가: 개입 조건의 재생 길이가 고정이 아니게 됐다.** 위 §4 표를 참고하고, 노출 시간을
    분석에 쓸 때 `T_REWIND` 를 분리할 것. 시청 조건은 영향이 없다(되감기 자체가 없다).

    되돌리려면 세탁은 장면 5 의 `dur: null` 을 `dur: 6` 으로 되돌리고 버튼 오버레이를 빼면
    되고, 게임은 `rewind_watch` 진입 뒤 1초 타이머로 `rewind_back` 을 부르면 된다.

12. **세탁 광고에만 나래이션을 넣었다. 감수한 교란이다.**

    세탁 자극이 장면 자막을 목소리로도 읽는다(`voice.js` · SPEC 4-3). **게임 자극에는
    목소리가 없다.**

    | | 세탁 | 게임 |
    |---|---|---|
    | 목소리 | 장면마다 자막을 읽는다 (13개 클립) | 없음 |
    | 읽는 말 | 자막과 글자 단위로 같다 (`npm test voice`) | — |
    | 길이 영향 | 없음. watch 31초 / intervene 49초 고정분 그대로 | — |
    | 끄는 법 | `sound=0` — 효과음·배경음과 같은 스위치 | 〃 |
    | 기록 | `VOICE_OK` · `VOICE_SPOKEN` | 필드 없음 |

    **왜 감수한 교란인가.** §3에 적어 둔 배경음 문제와 같은 종류인데 더 세다. 사람 목소리는
    31~55초 내내 깔리는 배경음보다도 주의를 끌고, 브랜드에 인격을 주고, 정보 이해를 돕는다.
    따라서 **블록 C·D(광고 태도·제품 평가)에서 자극 간 차이가 나올 때 그게 광고 형식
    때문인지 목소리 때문인지 이 설계로는 갈리지 않는다.** §3의 배경음 경고가 여기에도
    그대로, 더 강하게 적용된다.

    **대안이 있었고 고르지 않았다.** 게임 자극에도 같은 성우·같은 톤으로 같은 서사 자리
    (도입·실패·되돌리기·제품 카드)에 목소리를 넣으면 평행성이 유지된다. 작업량과 게임 쪽
    타임라인 손질을 이유로 세탁만 넣기로 정했다. **파일럿에서 두 광고의 기저 차이를 볼 때
    이 항목을 배경음과 함께 확인할 것** — 차이가 크면 그때 게임 쪽에 넣거나 세탁 쪽을 뺀다.

    **조건 간에는 비대칭이 없다.** 나래이션은 자막을 그대로 읽고 자막은 재생 목록을 따르므로,
    watch 는 다섯 문장, intervene 은 열 문장을 듣는다. 이는 §5-1 의 이미 알려진 노출 비대칭과
    같은 것이고 새로 생긴 것이 아니다.

    **되돌리려면** `index.html` 에서 `voice-clips.js`·`voice.js` 두 줄을 빼면 된다 —
    `scenes.js` 는 목소리가 없으면 무음 대역을 잡고 자극은 그대로 재생된다.

13. ~~광고에서 나가는 길이 [구매하기] 하나뿐이다~~ **해소됨.** 두 자극의 제품 화면에
    **오른쪽 위 닫기 [×]** 를 넣었다.

    **왜 문제였나.** 화면에 누를 수 있는 것이 CTA 하나뿐이면 그 클릭이 "사고 싶다"가
    아니라 **"나가려면 이것밖에 없다"** 가 된다. `CTA_CLICK` 이 이 연구의 종속변인이라
    이건 생태학적 타당성 문제이기 전에 측정 문제다. 8초 뒤 저절로 끝나긴 했지만,
    화면이 "무조건 눌러야 하는 화면"으로 읽히는 것 자체가 클릭률을 밀어 올린다.

    | | 세탁 | 게임 |
    |---|---|---|
    | 어디 | 장면 10 오른쪽 위 | 제품 카드 오른쪽 위 |
    | 크기·자리 | `top 2.4cqh · right 3.2cqw · 7.2cqh` | 〃 (같은 cq 값) |
    | 소리 | `beat` (가장 작은 신호) | 〃 |
    | 기록 | `CLOSE_CLICK` · `T_CARD` | 〃 |

    **두 자극에 같이 넣었다.** 한쪽에만 있으면 그 자극에서만 CTA 가 강제되어,
    `CTA_CLICK` 의 자극 간 차이가 광고 형식이 아니라 "나갈 수 있었는가"가 된다.
    크기·자리를 같은 cq 값으로 맞춘 것도 같은 이유다 — 한쪽 [×] 가 더 크거나 눈에
    잘 띄면 그만큼 그쪽 CTA 가 덜 눌린다.

    **닫기에도 소리를 낸다.** CTA 만 청각 보상(`cta`)을 갖고 닫기가 조용하면 그 차이가
    CTA 쪽으로 미는 힘이 된다. 보상이 아니라 눌렸다는 확인이므로 큐 목록에 새 신호를
    만들지 않고 **가장 작은 신호(`beat` 0.16)** 를 쓴다 — `SFX-CORE` 는 그대로다.

    **연출로도 한쪽을 밀지 않는다.** 세탁 쪽 `.cta` 에는 예전부터 등장 애니메이션이
    걸려 있었다. 닫기만 가만히 놓여 있으면 CTA 가 먼저 눈에 들어오므로 **같은
    애니메이션·같은 지연**을 준다. 게임 쪽은 카드에 등장 연출이 없어 둘 다 그냥 뜬다.
    `npm test motion` 의 규칙 F 가 두 버튼의 등장 방식이 같은지 검사한다.

    **대가: 제품 메시지 노출 시간이 참가자마다 달라진다.** 예전에는 CTA 를 안 누르면
    누구나 8초를 다 봤다. 이제 바로 닫으면 1초도 안 볼 수 있다. **`T_CARD` 로 분리해서
    기록하므로** 분석에서 제품 메시지 노출을 쓸 때 이 값을 쓸 것 —
    `T_REWIND` 를 `DWELL_TOTAL` 에서 분리하는 것(§11)과 같은 규칙이다.
    이야기 구간(장면 1~9)의 노출은 영향받지 않는다. [×] 는 마지막 화면에만 있다.

    **분석에서 세 갈래가 갈린다**: `CTA_CLICK=1`(구매 의향) · `CLOSE_CLICK=1`(적극적 회피) ·
    둘 다 0(무반응·시간 경과). 예전에는 뒤의 둘이 한 칸에 뭉쳐 있었다.

14. ~~세탁의 장면 길이를 나래이션에 맞춰 잘랐다 — 두 자극의 노출 시간 대칭이 깨졌다~~
    **해소됨.** 두 자극을 양쪽에서 좁혀 다시 맞췄다.

    | | 세탁(자르기 전) | 세탁(자른 뒤) | 세탁(지금) | 게임(전) | 게임(지금) |
    |---|---|---|---|---|---|
    | watch | 31.2초 | 18.6초 | **28.0초** | 31.2초 | **28.0초** |
    | intervene | 59.6초 | 37.0초 | **49.7초** | 55.4초 | **49.7초** |
    | 제품 메시지 | 8초 | 8초 | 8초 | 8초 | 8초 |

    **왜 재기만 하고 넘어갈 수 없었나.** §5-3 의 청각 밀도는 "맞추지 않고 재기로" 했다.
    그 처리가 통한 이유는 `SFX_COUNT` 가 참가자마다 다르기 때문이다. 노출 시간은 다르다 —
    세탁 참가자는 전원 짧고 게임 참가자는 전원 길어서 **자극과 1:1로 공선**이다.
    `DWELL_TOTAL` 을 공변량에 넣어도 자극 효과와 분리되지 않는다. 즉 이 자리에서
    "로그에 남으니 분석에서 빼자"는 성립하지 않고, 자극 자체를 고치는 수밖에 없었다.

    **어느 쪽도 혼자서는 못 맞춘다.** 각각의 하한을 먼저 쟀다.

    | | 무엇이 하한을 정하나 | 하한 |
    |---|---|---|
    | 게임 | 자동 조종이 맞게 고르다 오판해 실패하는 각본 + 되감기가 첫 오판보다 앞으로 가야 한다는 제약 | watch 28초 안팎 |
    | 세탁 | 나래이션 클립 + 0.3초 | watch 18.6초 |

    게임을 세탁(18.6초)까지 줄이려면 실패까지의 타임라인 15.6초를 5초대로 만들어야 하는데,
    그러면 자동 조종의 **첫 선택부터 오판**이 되어 "유능하던 인물이 판단을 잘못한다"는
    각본이 사라진다. 그래서 게임은 하한까지만 줄이고 나머지를 세탁이 올라와 만났다.

    **게임 쪽에서 줄인 것** (watch −3.2초 · intervene −5.7초)

    | 자리 | 전 | 후 | 근거 |
    |---|---|---|---|
    | 도입부 사건 2개 | — | 걷어냄 | 순증이 0(+7 게이트 · −7 적)이라 `START_POWER` 가 그대로다. 실패 지점 18.2 → 15.6초 |
    | 구출 길이 `RESCUE_SEC` | 20.3초 | 18.0초 | 되감은 지점에서 실패 지점을 2.0초 넘긴 자리. 더 줄이면 실패 지점 전에 끝난다 |
    | 되감기 바닥 `REWIND_FLOOR_SEC` | 3.0초 | 2.0초 | 되감아 돌아가는 자리의 **파워를 23 으로 유지**하기 위해서다. 3.0 이면 첫 게이트(+9)가 이미 지나가 구출이 그만큼 쉬워진다 |
    | 결과 연출 → 카드 사이 | 3.5 / 2.5초 | 2.7 / 1.9초 | 연출이 아니라 그 뒤 화면이 서 있던 시간이다 |

    구출 난이도는 §5-7 에서 다시 쟀고 양 끝(주의 100% · 무조작 0%)은 그대로다.
    `POWER_END = 7` 도 그대로다. `GATES_PASSED` 만 watch 기준 9 → 8 로 바뀐다.

    **세탁 쪽에서 늘린 것** (watch +9.4초 · intervene +12.8초)

    | 장면 | 전 | 후 | 늘린 시간이 하는 일 |
    |---|---|---|---|
    | 1 목표 | 2.94 | 4.00 | 내일 입을 옷과 오늘의 빨래를 한 화면에 두는 설정 샷 |
    | 2 제품 미사용 | 1.58 | 2.60 | 아무것도 넣지 않고 문이 닫히는 것을 보게 한다 |
    | 3 실패 진행 | 4.00 | 6.00 | 드럼이 돌고 물이 물드는 것을 말없이 보는 시간이 불안을 쌓는다 |
    | 4 실패 결과 | 2.08 | 7.40 | 광고의 경첩. 클로즈업으로 다시 잡은 얼룩을 읽을 시간이다 |
    | 7 제품 작동 | 2.44 | 3.90 | 시트가 염료를 붙잡는 과정 자체가 제품 설명이다 |
    | 8 개선 결과 | 1.29 | 3.25 | 클로즈업. 색이 그대로임을 확인하는 데 시간이 든다 |
    | 9 · 10 | 4.00 · 8.00 | 그대로 | 이미 그림이 일하는 장면이다 |

    **늘린 시간에 할 일이 없으면 그건 예전의 빈 시간이다.** 장면 8 이 실제로 그랬다 —
    장면 4 와 같은 클로즈업인데 `.sc8` 에는 카메라도 옷 움직임도 없어서, 3.25초 중 2.26초가
    얼어붙은 그림이 된다. 장면 4 와 **반대 방향**의 움직임을 새로 넣었다(장면 4 는 다가가며
    옷이 처지고, 장면 8 은 물러나며 옷이 올라온다). 값은 장면 4 보다 작다 — 해소를 크게
    움직이면 제품 효과를 과장하는 연출이 된다(SPEC 2장).

    **"클립 + 0.3초" 규칙은 이제 하한이다.** 길이를 정하는 것은 자극 간 대칭이고,
    나래이션은 그 안에 들어가되 말이 끝난 뒤에도 화면이 남는다. `voice.test.js` 의 검사도
    "말 끝나고 0.3초 안에 컷" 에서 "클립 + 0.3초가 장면 안에 들어간다 + 합계가 게임과 맞는다"로
    옮겼다 — 클립을 다시 굽든 `DUR` 을 손보든 대칭이 깨지면 거기서 걸린다.

    **대신 옮겨 온 대가 둘.**

    - **청각 밀도 차이가 벌어졌다(2.5배 → 3.5배).** 세탁은 신호 수가 장면 수로 정해져 있어
      길이를 늘려도 5회·11회 그대로인데 시간만 늘었고, 게임은 사건이 줄어 신호도 줄었다.
      맞추지 않고 재기로 한 §5-3 의 결정은 그대로다(`SFX_COUNT` 가 참가자마다 남는다).
    - **세탁 쪽 완성도가 또 한 번 올라갔다.** 장면 8 의 움직임은 게임에 대응물이 없는
      개선이다 — §5-16 에 적은 완성도 비대칭이 그만큼 커진다.

    **되돌리려면** `scenes.js` 의 `DUR` 표와 게임의 `MAIN_TIMELINE_A|B` · `RESCUE_SEC` ·
    `REWIND_FLOOR_SEC` · 카드 지연을 함께 돌려야 한다. 한쪽만 돌리면 대칭이 깨진다.
    길이를 다시 만질 때 확인할 것: ① 장면 10 = 게임 제품 카드 = 8초 ② 자동재생 대기
    (SPEC 4-3장 — 소리가 정해지기 전에는 시작하지 않는다) ③ 배경음 마디(9.6초 주기)와
    장면 경계가 겹치지 않는지 ④ 게임의 `START_POWER` 순증 계산.

15. **광고다운 연출을 두 가지 넣었다 — 브랜드가 붙은 시트, 제품명을 스치는 빛.**

    자극이 "실험 자료"처럼 보이고 광고로 안 읽힌다는 판단에서 나온 것이다.
    둘 다 **자극 간 비대칭을 만들지 않는 선**에서 골랐다.

    **① 시트에 브랜드를 적었다(세탁 전용 · 대응물 없음).** 예전 시트는 동그란 마크와
    물결선뿐이라, 개입 조건 참가자가 세탁기에 끌어다 넣는 물건이 **무엇인지 화면에서
    읽히지 않았다** — 브랜드는 선반 위 상자에만 있었고 그건 배경 소품이다. 참가자가
    직접 다루는 유일한 물건이 이 시트이므로, 넣는 동안 이름이 보여야 "클린가드를
    넣었다"가 된다. `ART.sheet` 한 곳을 고쳤고 장면 6(끌어다 놓기)·7(작동)·10(팩샷)이
    같이 바뀐다.

    비대칭이 아닌 이유: 게임 자극에는 시트에 해당하는 물건이 없다(개입이 잠수정
    조종이다). 대응물이 없는 자리라 맞출 대상 자체가 없고, **watch·intervene 양쪽이
    같은 그림을 본다** — 장면 7·10 은 공통 장면이고 장면 6 은 watch 도 시연으로 본다.
    조건 간 차이는 생기지 않는다.

    **② 제품명 위를 빛이 한 번 스친다(두 자극 모두).** 광고에서 제품 이름이 한 번
    반짝이는 그 문법이다. kokonut UI 의 `shimmer-text` 를 보고 가져왔는데, **컴포넌트를
    가져오지 않고 연출만 옮겼다** — 그쪽은 React + Tailwind + Motion 이고 세탁 자극은
    번들러도 의존성도 없다(§5-12 의 오프라인 요건). CSS 로 옮기면 `?still` 스토리보드
    정지와 `npm test motion` 의 규칙 A~F 가 그대로 살아 있다.

    | | 세탁 | 게임 |
    |---|---|---|
    | 어디 | 장면 10 제품명(`.s10-sheen`) | 제품 카드 이름(`.pc-name::after`) |
    | 그리는 법 | SVG · `clipPath` 안에서 움직이는 그라디언트 띠 | DOM · `background-clip: text` 로 오려 낸 그라디언트 |
    | 걸리는 곳 | **글자에만** | 〃 |
    | 지연 · 길이 | 1.0초 · 0.9초 | 〃 |
    | 횟수 | **한 번** | 〃 |
    | 기울기 · 색 | 약 14° · 흰색 0.9 | 〃 |

    **글자에만 거는 것도 맞춰야 했다.** 처음에는 게임 쪽을 이름의 네모 칸 위로 띠가
    지나가게 짰는데, 그 카드가 어두워서(`#0b3a56` → `#02101c`) 글자뿐 아니라 배경까지
    하얗게 밀렸다. 세탁 카드는 연한 민트라 같은 띠가 글자만 밝히고 배경은 거의 안
    변한다 — **같은 값을 넣었는데 눈에 띄는 정도가 반대로 갈린 것이다.** 두 쪽 다
    글자 모양으로 오려 내도록 고쳤다. 값이 같은 것과 보이는 것이 같은 것은 다르다.

    **한 번만 도는 것이 핵심이다.** 무한 반복으로 두면 반짝인 횟수가 카드가 떠 있는
    시간에 비례한다. 지금은 두 카드가 다 8초지만(§5-14 에서 되돌렸다) 길이는 언제든
    다시 갈릴 수 있는 값이고, 한 번으로 못 박으면 카드 길이가 달라도 반짝임은 정확히
    한 번씩이다.

    **CTA 버튼에는 안 넣었다.** kokonut 의 `gradient-button`·`particle-button` 이
    후보였는데, `CTA_CLICK` 이 이 연구의 종속변인이라 버튼의 눈에 띄는 정도를 올리면
    클릭률의 절대값이 통째로 움직인다. 양쪽에 똑같이 넣으면 비교는 유지되지만 기저율이
    바뀌므로, **종속변인에 직접 닿는 자리는 건드리지 않는다**는 쪽을 택했다.
    `particle-button` 은 애초에 쓸 수 없다 — 두 자극 모두 CTA 를 누르는 즉시 끝나서
    입자가 터질 시간이 없다.

    검토했다가 뺀 것: `type-writer`(자막을 한 글자씩 치면 정보 전달이 늦어지고
    나래이션과 어긋난다) · `background-paths`·`beams-background`(움직이는 배경은
    장면 연출과 싸우고 각성을 올린다) · `card-flip`(장면 9 전후 비교에 참가자
    상호작용이 생긴다) · `attract-button`(버튼이 커서를 따라오면 누르는 난이도가
    참가자마다 달라진다).

16. **세탁 장면 4·8·9 를 클로즈업으로 다시 잡았다 — 완성도 비대칭이 커졌다.**

    무엇을 왜 고쳤는지는 SPEC 3장에 적었다(옷이 몸통 위에 겹쳐 "입은 것"으로 읽히던
    구도 · 화면 폭 6분의 1이던 얼룩 · 차이를 찾아야 보이던 전후 비교 · 옷 밖으로
    삐져나오던 얼룩). 여기 적을 것은 **자극 간 문제** 하나다.

    §5-9 에 "두 광고의 완성도가 이제 비슷한지는 결국 사람이 봐야 한다"고 적어 두었다.
    이 작업은 **세탁 쪽만 눈에 띄게 좋아지는 변경**이다. 게임 자극에는 옷도 클로즈업할
    대상도 없어서 대응하는 수정이 없다. 광고 태도(블록 C)에서 세탁이 높게 나오면
    그것이 광고 형식의 효과인지 **한쪽만 더 공들인 결과**인지 지금 로그로는 안 갈린다.

    맞추려고 게임에 억지로 연출을 넣지는 않았다 — 대응물이 없는데 비슷한 무게의
    무언가를 지어내면 그건 또 다른 임의 변경이다. 파일럿에서 블록 C 의 자극 간 기저
    차이를 볼 때 이 변경 이후 값인지부터 확인할 것.

    조건 간(watch·intervene) 비대칭은 없다. 장면 4 는 두 조건이 함께 보는 장면이고,
    8·9 는 원래부터 intervene 전용이라 조건 대비가 달라지지 않았다.
