# SPEC — 이염 방지 세탁 시트 실패-수정형 광고 자극 (실험용)

Claude Code로 이 파일을 프로젝트 루트에 두고 구현 지시의 기준으로 삼는다.
근거 문서: 실패-수정형 광고 제작 가이드라인(교수 제공) 7장 적용 예시 · 도구 6~8 · 9절 점검표.

## 1. 산출물

단일 웹앱 `index.html` (+ `scenes.js`, `style.css`, `sfx.js`, `voice.js`, `voice-clips.js`) — 모바일 세로 9:16 전체화면.
프레임워크 없이 순수 HTML/SVG/CSS/JS. 외부 네트워크 요청 없음(오프라인 실행 가능해야 함).

URL 파라미터로 4가지 변형을 모두 커버:
- `?mode=watch` 시청형(실패까지만 보고 종료) / `?mode=intervene` 개입형(되감기 후 참가자가 수정)
- `?ver=A` 밝은색 셔츠 + 짙은 색 양말 / `?ver=B` 밝은색 수건 + 짙은 색 티셔츠
- `?sid=` 익명 세션 ID (로그에 그대로 기록, 미지정 시 랜덤 생성)

즉 실제 자극 4종(watch/intervene × A/B)이 한 코드베이스에서 나온다.

## 2. 고정 원칙 (위반 시 재작업)

- **조건별 재생 목록** — watch는 `1,2,3,4,10`(실패까지 보여주고 제품 메시지로 종료), intervene은 `1~10`(실패 → 되감기 → 참가자 개입 → 개선). 장면 정의는 `SCENES` 하나뿐이고 `PLAYLIST`가 어떤 장면을 재생할지만 고른다.
- 두 조건이 **공유하는 장면(1·2·3·4·10)은 그림·문구·길이·정보량이 픽셀 단위로 동일**해야 한다(같은 렌더 함수 공유로 강제). 조건별 장면을 따로 만들지 않는다.
- 알려진 교란: watch는 고정 10.8초, intervene은 16.2초 고정 + 되돌리기 대기 + 되감기 6초 + 장면 6 조작 시간이라 **노출 시간과 정보량이 조건 간에 다르다.** intervene만 제품 작동·개선 결과·전후 비교를 본다. 분석·해석에서 이 비대칭을 반드시 감안한다.
- 가상 브랜드 사용: 브랜드명 "클린가드"(가칭, 상수로 분리해 교체 가능). 실존 브랜드·로고·상표 금지.
- 효과 표현 제한: "이염을 **줄여** 준다"까지만. "완전 방지", "100%", 모든 염료 제거 표현 금지. 장면 8의 개선 결과는 "원래 색에 가까운 상태"이지 완전 무결이 아니다.
- 인물을 무능하게 묘사하지 않는다. 실패 원인은 "시트를 넣지 않음" 하나로 제한(세탁기 고장·잘못된 코스 등 다른 원인 배제).
- 실패의 심각도: 일상적 불편 수준. 문구 예: "내일 입으려던 셔츠인데…" — 면접 실패·경제적 손실 등 과장 금지.

## 3. 장면 정의 (가이드라인 도구 6 · 7.5 장면표 구현)

길이: watch 10.8초 고정(2.94+1.58+1.16+2.08+3.07), intervene 16.2초 고정 + **장면 5 되돌리기 대기 + 되감기 6초** + 장면 6 조작 시간. 각 장면은 SVG 일러스트 + 짧은 자막 1줄 + CSS 트랜지션.

**장면 길이는 나래이션이 정한다 — 클립 길이 + 0.3초다**(`scenes.js` 의 `DUR` 표).
예전에는 4·5·6·8초로 고정돼 있었고 말은 1~3초에 끝나 버려, 말이 끝난 뒤 장면마다
1.4~6.2초가 비어 있었다(장면 3 은 0.86초 말하고 5.14초를 더 서 있었다). 지금은 말이
끝나면 바로 컷이다. A·B 가 갈리는 장면(1·4)은 **긴 쪽 클립**에 맞춰 두 버전의 길이를
같게 유지한다. 장면 안의 애니메이션은 CSS 변수 `--scene-dur` 로 이 길이를 받아 같이
줄어든다 — 초를 그대로 적어 두면 카메라가 목적지에 닿기 전에 컷이 난다.

**대가는 노출 시간이다.** watch 31초 → 10.8초, intervene 55초 → 27초 안팎으로 짧아졌고,
게임 자극(31.2초 · 55.4초)과의 길이 대칭이 깨졌다(INTEGRATION.md §4).
장면 10 의 CTA 노출도 8초 → 3.07초다.

| # | 장면 | 내용 (ver A 기준 / B는 소재만 교체) | 길이 | 자막 |
|---|---|---|---|---|
| 1 | 목표 | **클로즈업** — 밝은 셔츠와 짙은 양말을 한 손에 하나씩 들고 있다. 어긋난 대각선 배치, 얼굴은 프레임 밖 | 2.94s | "내일 입을 셔츠, 오늘 같이 빨래하기" |
| 2 | 제품 미사용 | 세탁기에 옷 투입. 옆에 시트 상자가 있지만 사용하지 않음(상자는 배경에 자연스럽게) | 1.58s | "그대로 세탁 시작" |
| 3 | 실패 진행 | 세탁기 창 안, 짙은 색에서 염료가 물에 번지는 단순 애니메이션 | 1.16s | "세탁 중…" |
| 4 | 실패 결과 | **클로즈업** — 물든 셔츠가 화면을 채우고 손이 어깨를 잡고 있다. 얼굴은 프레임 밖 | 2.08s | "셔츠 색이 변해 버렸다" |
| 5 | 되감기 | 실패 화면이 멈춰 있고 **[되돌리기] 버튼**이 뜬다. 누르면 화면 전체 역재생 연출(장면 4→3→2 빠른 역방향), 세탁 시작 직전에 정지 | 가변(누를 때까지) + 6s | "세탁을 시작하기 전에, 이 제품을 사용했다면?" |
| 6 | **수정 행동** (intervene 전용) | 시트 1장이 화면 하단에 강조 표시(시트에 브랜드명이 적혀 있다 — 참가자가 넣는 물건이 무엇인지 화면에서 읽혀야 한다), 세탁기 투입구가 점선 하이라이트 | 가변(드롭 성공 시 진행) | "시트를 세탁기 안으로 끌어다 놓아 주세요" |
| 7 | 제품 작동 | 세탁기 창 안: 번지던 염료 입자가 시트 쪽으로 이동해 흡착되는 단순화 애니메이션 + "기능 설명 화면" 라벨(설명용 시각화임을 표시) | 2.44s | "떠다니는 염료를 시트가 붙잡아요" |
| 8 | 개선 결과 | **같은 클로즈업** — 같은 셔츠가 원래 색에 가깝다. 장면 4 와 구도·배율이 같다 | 1.29s | "색은 그대로" |
| 9 | 결과 비교 | 좌우 분할: 시트 없이(물든 셔츠) vs 시트와 함께(원래 색). **옷만 크게** — 동일 구도·동일 셔츠 | 1.63s | "시트 하나의 차이" |
| 10 | 제품 메시지 + CTA | 제품 팩샷 + 핵심 문구(제품명 위를 빛이 한 번 스친다) + CTA 버튼 + **오른쪽 위 닫기 [×]** | 3.07s 후 자동 종료(둘 다 미클릭 시) | "세탁 중 이염을 줄여 밝은 옷을 보호하세요" / 버튼: [지금 구매하기] · [×] |

**장면 1 은 클로즈업이다 — 장면 2 와 같은 그림이었다.**

예전에는 1 과 2 가 **같은 거리·같은 자리에 선 인물·같은 세탁기**였고 차이는 말풍선과
양말 위치뿐이었다. 광고가 똑같은 프레임 두 장으로 시작하고 있었던 것이다. 지금은
1 이 클로즈업(무엇이 위험한지), 2 가 와이드(어디서 무슨 일이 벌어지는지)다.

이 광고의 전제는 "밝은 옷과 짙은 옷을 **같이** 빤다"이고, 그 둘을 나란히 든 그림이면
첫 컷에 전제가 다 들어간다. 짙은 옷을 왼쪽 위·밝은 옷을 오른쪽 아래로 어긋나게 놓아
대각선을 만든다 — 같은 높이로 나란히 두면 둘이 대등해 보이고 세로 화면 아래가 통째로
빈다. 크기 차이(3.6 : 4.4)가 **밝은 옷이 주인공**이라는 것도 같이 말한다.

말풍선은 뺐다. 얼굴이 없는 컷에서 말풍선은 가리킬 대상이 없고, 담고 있던 문구
("내일 입을 셔츠")는 자막이 그대로 갖고 있어 **정보량은 안 바뀐다.**

덤으로 이 컷이 광고 전체에서 **처음으로 짙은 색 덩어리**를 화면에 들여왔다. 열 장면이
전부 옅은 민트·흰색·베이지라 눈이 붙잡을 곳이 없었다.

**장면 4·8·9 도 클로즈업이다 — 옷이 화면을 채우고 인물은 손만 남는다.**

예전에는 셋 다 방 전체가 보이는 와이드 샷이었고 인물이 옷을 가슴 앞에 들고 있었다.
그 구도에서 두 가지가 깨져 있었다. ① **옷이 몸통 위에 겹쳐 그려져 "들고 있다"가 아니라
"입고 있다"로 읽혔다.** ② 정작 봐야 할 얼룩이 화면 폭의 6분의 1이었다 — 이 장면들의
일은 "색 변화가 한눈에 보이게"인데 그걸 방 건너편에서 보고 있었다. 장면 9(전후 비교)는
같은 포즈·같은 색의 인물 둘이 나란히 서 있어서 **차이를 찾아야 보였다.**

얼굴을 뺀 것은 연출 판단만이 아니다. 표정으로 실패를 전하면 그 실패가 **사람의 실수**로
읽히는데, 원인은 "시트를 안 넣은 것" 하나여야 하고 인물을 무능하게 그리지 않는 것이
2장의 고정 원칙이다. 옷이 주인공이면 탓이 염료로 간다. 감정은 옷이 처지는 연출
(`s4-garment-check`)이 맡는다. 장면 9 에서도 얼굴이 빠지면서 좌우에 남는 차이가
**얼룩 하나뿐**이 되어, "제품 사용 여부 외 조건이 동일"이라는 수용 기준이 더 엄격해졌다.

같이 고친 것: **얼룩이 옷 모양 밖으로 삐져나오고 있었다.** 얼룩 타원이 아무 데도 안
잘려서 소매 사이 빈 곳에서는 인물의 몸통 위에 그려졌다. 멀리서 볼 때는 안 보였고
카메라를 붙이자 드러났다. 이제 보이는 몸판과 얼룩이 **같은 path 로 만든 clipPath** 를
쓴다 — 옷 모양을 고치면 얼룩이 잘리는 범위도 같이 따라간다.

**이염 얼룩을 다시 그렸다.** 예전에는 타원 다섯 개였고, 클로즈업으로 바꾸자
**물방울이나 비눗방울로 읽혔다.** 세 겹으로 짠다.

| 겹 | 무엇 | 왜 |
|---|---|---|
| ① 전체 농도 | 옷 전체에 세로 그라디언트(위 0.05 → 아래 0.26) | 이염의 첫인상은 점이 아니라 **흰색이 탁해진 것**이다. 이 겹이 없으면 "깨끗한 옷에 점이 묻었다"가 된다. 물에 잠겨 있던 아래가 더 짙다 |
| ② 번진 덩어리 | 불규칙한 덩어리 5개 · 흐린 후광 + 본체 + 비껴 놓은 속 | 세 겹이 같은 중심·같은 모양이면 과녁처럼 보인다 |
| ③ 잔 얼룩 | 작은 것 8개 | 크기가 고르면 오염이 아니라 무늬가 된다 |

**모양은 난수가 아니라 고정 표로 만든다.** 각도별 반지름 배율표를 두고, 겹마다
시작점만 옮겨 다른 모양을 얻는다. 난수를 쓰면 참가자마다 얼룩이 달라져 같은 자극이
아니게 된다. 표 값을 1 기준으로 부풀리는 것(`amp`)이 핵심이다 — 표를 그대로 쓰면
거의 원이고, 점 수가 적은 채로 값을 번갈아 크게 주면 **둥근 사각형**이 된다.

**클로즈업 배경에 비네트를 깔았다.** 벽도 옷도 거의 흰색이라 붙여 놓으니 옷의
실루엣이 배경에 묻혔다. 가운데를 밝히고 가장자리를 눌러 옷이 뜨게 한다 — 광고 조명이
제품 뒤를 어둡게 까는 것과 같다.

ver B 교체 사양: 셔츠→수건, 양말→티셔츠, 인물 옷 색상과 배경 색조만 변경. 구도·장면 수·길이·문구 구조는 동일(도구 8 평행 자극 요건).

## 4. 장면 6 상호작용 사양 (intervene 모드)

- 시트 요소: pointerdown/move/up으로 드래그. 터치·마우스 모두 지원.
- 드롭 존: 세탁기 투입구 영역. 성공 판정 시 시트가 안으로 들어가는 짧은 애니메이션 → 세탁기 문 닫힘 → 장면 7 자동 진행.
- 오답 처리: 드롭 존 밖에서 놓으면 시트가 원위치로 스냅백(비난 문구 없음).
- 무조작 안내: 8초 무조작 시 손가락 아이콘이 시트→투입구 경로를 시연하는 힌트 애니메이션 반복(계획서 8항 "일정 시간 미조작 시 안내" 이행). 힌트는 정답을 대신 수행하지 않음.
- 장면 6은 intervene 전용이다. watch 재생 목록에는 없다. (인물의 손이 시트를 넣는 시연 애니메이션과 `WATCH_S6_MS` 상수는 `?still=6`·조건 되돌리기용으로 코드에 남아 있으나 기본 재생에서는 쓰이지 않는다.)
- 조작 안내 UI(점선 드롭존 링 `.s6-ring`, 드래그 경로 `.s6-guide`, 힌트 손가락 `.s6-hand`)는 intervene 전용이다. 조작할 수 없는 UI를 watch 참가자에게 보여주면 자극 외 변량이 된다. 그림 자체는 두 조건이 같은 함수로 렌더하고 CSS(`.sc6.is-watch`)에서 표시만 끈다 — 그래서 "같은 렌더 함수" 요건은 유지된다.
- OS "동작 줄이기(prefers-reduced-motion)" 설정은 애니메이션 속도·입자 수를 바꾸지 않는다(참가자 간 자극 동일성). 설정값은 `REDUCED_MOTION`에 기록만 한다.

### 4-1. 효과음

`sfx.js`가 Web Audio로 그때그때 합성한다 — 오디오 파일이 없으므로 오프라인 실행 조건은 그대로다.

- 신호는 열 가지(`start · beat · bad · fail · rewind · grab · miss · success · card · cta`)이고, **길이·세기·마스터 음량·CTA 클릭음은 게임 자극(`game/src/sfx.js`)과 완전히 같은 코드**에서 나온다. 두 파일의 `SFX-CORE` 구간이 글자 하나까지 같아야 하며 `npm test sfx`가 이를 검사한다. 자극마다 다른 것은 음색(`VOICES`)뿐이다.
- 기본값은 켜짐. `?sound=0`이면 처음부터 끝까지 무음이다.
- 브라우저 자동재생 정책 때문에 제스처 없이는 소리가 나지 않을 수 있다. 러너가 iframe의 잠금을 대신 풀고(`preview.html`), 실제로 소리가 났는지는 `AUDIO_OK`에 참가자마다 기록한다.
- **배경음(BGM)** 도 같은 파일이 합성한다. 자극 시작에 켜고 종료에 끈다(페이드 인 1.6초 · 아웃 0.9초).
  음량 천장·페이드·켜고 끄는 절차는 게임 자극과 **같은 코어(`BED`)** 이고, **음색(`BED_VOICE`)만 다르다** —
  세탁은 방 공기 + 다장조 루프, 게임은 가단조 루프. **네 화음(C·Am·F·G)도 빠르기(100bpm)도
  한 바퀴 길이(9.6초)도 같고**, 조성과 음표의 성김만 다르다 — 세탁은 4분·2분음표, 게임은
  8분음표 아르페지오. 자극별로 다르게 두기로 한 결정이고,
  그 대가는 INTEGRATION.md §3 에 적어 두었다. 배경음이 효과음을 가리지 않도록 천장을 가장 작은
  신호(`beat` 0.16)의 1/3 아래(0.055)에 두고, `npm test sfx`가 이 관계를 검사한다.
- `?sound=0`이면 효과음과 배경음이 함께 꺼진다. 잠금이 늦게 풀려도 배경음은 그 순간 자동으로 켜진다.
- `AudioContext`가 없는 환경(테스트·구형 브라우저)에서는 무음으로 떨어지고 자극은 그대로 재생된다.

### 4-2. 되돌리기 버튼 (장면 5 · intervene 전용)

되감기는 **저절로 시작하지 않는다.** 실패 화면이 멈춘 채 [되돌리기] 버튼이 뜨고, 참가자가 누르면 그때 역재생이 시작된다.

- 왜: 광고가 알아서 되감으면 되돌리는 주체가 광고이고 참가자는 구경꾼이다. 이 연구가 재려는 것이 "내가 고쳤다"는 경험이므로, **고치기로 하는 첫 결정도 참가자가 내려야 한다.**
- 누르기 전까지 무한정 기다린다(장면 6과 같은 규칙). 버튼은 계속 맥동해 "여기를 누르면 된다"를 알린다 — 대신 눌러 주지는 않는다.
- 누르기까지 걸린 시간을 `T_REWIND`에 기록한다. watch에는 장면 5가 없으므로 `null`이다.
- 게임 자극(`game/`)에도 같은 자리에 같은 버튼이 있고 크기·문구 구조·눌림 반응을 맞춘다. 한쪽만 참가자가 누르게 하면 두 자극의 "주도권" 차이가 조작점검·통제감에 그대로 들어온다.
- 이 버튼 때문에 **intervene의 재생 길이가 참가자마다 달라진다.** 고정분은 16.2초이고 나머지는 대기 시간이다. 분석에서 노출 시간을 쓸 때 `DWELL_TOTAL`을 그대로 쓰지 말고 `T_REWIND`를 분리할 것.

### 4-3. 나래이션 (세탁 자극에만 있다)

장면 자막을 목소리로도 읽는다. `voice.js`가 재생하고 문장은 `voice-clips.js`에 base64로
심겨 있다 — 오디오 파일도 네트워크 요청도 없으므로 오프라인 요건(1장)은 그대로다.

- **읽는 말은 자막 그대로다.** 새 문구를 지어내지 않는다. 정보량이 자막과 어긋나지 않고,
  2장의 효과 표현 제한("줄여 준다"까지)을 새 문장이 넘을 일이 없다.
  `npm test voice`가 열 장면 × 두 조건 × 두 버전의 자막과 클립 문장을 글자 단위로 대조한다.
- **클립 길이가 곧 장면 길이다** — 각 장면은 클립 + 0.3초이고, 말이 끝나면 바로 컷이다
  (3장의 `DUR` 표). `npm test voice`가 양쪽으로 검사한다: 클립이 장면보다 길지 않고
  (말이 끊기지 않는다), 장면이 클립보다 0.3초 넘게 길지도 않다(빈 시간이 남지 않는다).
- 길이가 클립에 붙어 있으므로 **클립을 다시 구우면 `DUR` 표도 같이 고쳐야 한다.**
  `voice-clips.js` 를 빼서 목소리를 되돌리는 경우(§5-12) 길이는 그대로 유지된다 —
  표는 손으로 적힌 값이라 클립이 사라져도 참가자 전원이 같은 길이를 본다.
- 소리가 열리는 순간 열세 클립을 미리 해독해 둔다. 여유가 0.3초뿐이라, 재생 시점에
  해독하면 그 시간만큼 말이 늦게 시작해 끝이 컷에 잘린다.
- **소리가 막혀 있으면 광고를 아직 시작하지 않는다.** 자동재생이 막힌 채로 재생이
  굴러가면 그 사이 장면의 나래이션은 영영 못 듣는다 — 잠금이 풀릴 때 되살리는 것은
  "그 순간의 장면" 하나뿐이라 지나간 문장은 잃는다. 길이를 나래이션에 맞춰 줄인 뒤로는
  watch 가 10.8초라 **화면을 한 번 누르는 사이에 광고의 절반이 지나간다.**
  - 러너 안에서는 **2.5초 상한**을 둔다. 시청 조건 참가자는 광고가 끝날 때까지 화면을
    한 번도 안 건드릴 수 있어, 무한정 기다리면 그 참가자에게는 광고가 시작되지 않는다.
    러너는 참가자의 제스처를 들고 `AD_UNLOCK` 을 보내므로 실제 경로에서 이 대기는 거의 0이다.
  - 자극을 **단독 주소로 열었을 때는 제스처를 기다린다.** 심사·리허설 경로라 참가자가
    아니라 사람이 확인하는 화면이고, 그냥 시작하면 확인하는 사람이 나래이션을 못 듣는다.
- 잠금이 풀리는 순간 **그때 떠 있는 장면의 문장을 처음부터 다시 읽는다.** 지나간 문장을
  몰아 읽지는 않는다 — 화면과 말이 어긋나는 것이 더 나쁘다. 다시 읽은 것은
  `VOICE_SPOKEN` 을 늘리지 않는다(같은 문장이라 로그의 뜻이 달라지면 안 된다).
- 잠금 해제·`?sound=0`으로 끄기는 효과음과 같은 경로다. 목소리가 실제로 났는지는
  `VOICE_OK`에 따로 기록한다(재생 경로가 달라 한쪽만 막힐 수 있다).
- `sfx.js`의 `SFX-CORE`는 게임 자극과 글자 단위로 같아야 하므로 **코어에 넣지 않고
  파일을 분리했다.** `npm test voice`가 `voice.js`가 `AD_SFX`를 건드리지 않는지 검사한다.
- 목소리는 piper `ko_KR-kss-medium`으로 구웠다. 데이터셋(KSS)이 CC BY-NC-SA 4.0이라
  **비영리 연구에서만** 쓴다. 다시 굽거나 사람 목소리로 바꾸려면 `tools/build-voice.py`.
- **합성 난수를 끄고 굽는다.** 기본값으로 두면 같은 문장이 구울 때마다 다른 억양으로
  나오고(실측 180.7 / 196.9 / 204.2Hz — 2.1반음 폭), 낱말 하나만 다른 ver A/B 짝이
  억양까지 갈린다. 실제로 첫 판에서 **장면 4가 A 232Hz · B 182Hz 로 4반음 벌어져
  있었다** — 이 광고의 전환점 대사인데 버전마다 다른 무게로 들렸다는 뜻이다.
  끄면 두 번 구워도 바이트 단위로 같은 파일이 나오고, 표현력 손해도 없다
  (높낮이 폭이 오히려 넓다).

**장면마다 말투가 다르다.** 처음 구운 판은 열세 문장이 전부 같은 톤·같은 속도였다 —
F0 가 227~259Hz 한 밴드에 몰려 있었고 **실패 장면이 해소 장면보다 높았다.**
`tools/build-voice.py` 의 `PROSODY` 가 장면마다 말 속도·높이·끝맺음을 정한다.

| 장면 | 성격 | 속도 | 높이 | 끝 |
|---|---|---|---|---|
| 1 도입 | 일상적 | 0.98 | +0.2 | |
| 2 세탁 시작 | 무심하게 (그 무심함이 실패의 씨앗) | **0.93** 가장 빠름 | −0.4 | |
| 3 세탁 중 | 낮고 조용히 | 1.06 | −1.6 | |
| 4 실패 | **가장 낮고 가장 느리게** | **1.13** | **−3.0** | −1.6 |
| 5 되감기 질문 | 물음 — `?`인데 piper 가 안 올려서 직접 올린다 | 1.02 | +0.6 | **+5.0** |
| 6 조작 안내 | 또렷하게 | 1.04 | +0.2 | |
| 7 제품 작동 | 설명조 | 1.00 | −0.3 | |
| 8 개선 | **가장 밝게** | 0.95 | **+2.2** | +1.2 |
| 9 비교 | 확신 | 1.02 | +1.0 | |
| 10 제품 메시지 | 앞으로 나오게 | 0.96 | +0.6 | +1.0 |

- **음량은 성격에 안 쓴다.** 열세 개 전부 −18 LUFS 다(실측 편차 0.5dB). 특정 장면만
  크면 그 장면에서만 각성이 올라가고, 5~9 는 개입 조건만 보는 구간이라 그 차이가
  조건 간 각성 비대칭이 된다 — 연출의 규칙 D(4-4)와 같은 이유다. 임팩트는 높낮이·
  속도·프레즌스 EQ(3.1kHz)에서 낸다.
- 공통 장면의 높이 폭(3.6반음)이 개입 전용 구간(2.5반음)보다 넓다. `npm test voice` 가
  이 관계와 ver·mode 짝이 같은 처리를 받는지를 검사한다.
- 검사가 보는 값은 **잰 값이 아니라 실제로 건 값이다.** 구운 소리에서 F0 를 되재는
  방식은 안 쓴다 — 문장 안에서 F0 가 한 옥타브 넘게 움직이는 대사가 있어
  ("셔츠 색이 변해 버렸다"는 167~302Hz) "가운데 높이"가 안정된 값이 아니고, 자기상관과
  조화곱 스펙트럼이 같은 클립을 208Hz 와 167Hz 로 달리 읽었다. 옮기는 양 자체는
  순음으로 검증했다(200Hz → +7반음 → 301.3Hz).
- **게임 자극에는 나래이션이 없다. 감수한 교란이다** — INTEGRATION.md §5-12.
  목소리는 배경음보다 센 변수라, 블록 C·D(광고 태도·제품 평가)의 자극 간 차이를
  광고 형식의 효과로 단정하면 안 된다.

### 4-4. 연출 규율

연출(컷 전환·카메라·인물 동작·속도 램프)은 전부 `style.css`에 있고, `npm test motion`이
아래 다섯 가지를 규칙 자체를 읽어 강제한다. 마크업이 한 글자도 안 달라도 CSS 한 줄이면
조건마다 다른 화면이 나오기 때문에, 마크업 비교(`parallel.test.js`)만으로는 못 막는다.

| | 규칙 | 왜 |
|---|---|---|
| A | 공통 장면(1·2·3·4·10) 규칙에 조건·버전 판별자 금지 | 참가자마다 다른 화면이 나오면 안 된다 |
| B | 조건 판별자는 장면 6 안에만 | 조작 UI만 조건에 따라 다르다(4장) |
| C | 장면 애니메이션은 `.cam`·`.hint-on`을 거친다 | `?still` 스토리보드 캡처가 전환 도중으로 찍히지 않게 |
| D | intervene 전용 구간(5~9)이 공통 구간보다 화려해지면 실패 | 노출 시간 비대칭 위에 각성 비대칭이 얹히지 않게 |
| E | 규칙이 가리키는 class가 그림에 실제로 있다 | 이름이 어긋나면 연출이 조용히 사라지고 검사는 전부 통과한다 |
| F | 장면 10의 [구매하기]와 [×]가 같은 방식으로 등장한다 | 둘 다 종속변인이다. 한쪽만 눈에 띄면 그게 클릭률 차이가 된다 |

그 밖에 지키는 것:

- `blur`를 큰 레이어에 걸지 않는다 — 기기 성능에 따라 프레임이 튀면 참가자마다 자극이 갈린다.
- 컷 전환은 500ms 이내다 — 엔진이 그때 이전 장면을 지운다.
- `prefers-reduced-motion` 오버라이드를 두지 않는다 — 설정값은 `REDUCED_MOTION`에 기록만 한다.
- **장면 10의 두 버튼([지금 구매하기]·[×])은 같은 방식으로 등장하고 둘 다 맥동하지 않는다.**
  둘 다 종속변인이라(`CTA_CLICK`·`CLOSE_CLICK`) 한쪽만 눈에 띄면 그 연출이 곧 클릭률
  차이가 된다. 장면 5의 [되돌리기]가 맥동하는 것과 다른 이유다 — 그쪽은 종속변인이
  아니라 진행 조건이고, 재는 값도 "눌렀는가"가 아니라 "언제 눌렀는가"(`T_REWIND`)다.
  규칙 F 가 두 버튼의 등장 방식이 같은지를 검사한다.

## 5. 로깅 (행동 로그 시트 스키마와 1:1)

`window.AD_RESULT`에 기록하고, 종료 시 `window.parent.postMessage({type:'AD_DONE', payload}, '*')` + `localStorage['ad_log_'+sid]`에 JSON 저장(둘 다).

```json
{
  "sid": "", "mode": "watch|intervene", "ver": "A|B",
  "t_start": 0, "t_end": 0,
  "DWELL_TOTAL": 0.0,        // 초, 장면1 시작~종료(또는 CTA 클릭)
  "DWELL_INT": 0.0,          // 장면 6 체류(초). watch는 그 장면이 없어 null
  "INT_DONE": 0,             // 개입 성공 1/0 (watch는 공란 의미로 null)
  "INT_ATTEMPTS": 0,         // 목표 밖 드롭(스냅백) 횟수
  "T_FIRST_DRAG": null,      // 초, 장면 6 진입~첫 드래그 시작 (watch·미조작은 null)
  "T_MANIP": null,           // 초, 첫 드래그~성공 (미완료면 장면 이탈까지, watch는 null)
  "T_REWIND": null,          // 초, [되돌리기]가 뜬 뒤 누르기까지 (watch는 null)
  "HINT_SHOWN": 0,           // 힌트 노출 횟수
  "CTA_CLICK": 0,            // [지금 구매하기] 클릭 1/0
  "CLOSE_CLICK": 0,          // 오른쪽 위 [×] 클릭 1/0. 둘 다 0이면 3.07초 경과로 끝난 것
  "T_CARD": null,            // 초, 장면 10이 뜬 뒤 둘 중 하나를 누르기까지. 안 누르면 null
  "REDUCED_MOTION": 0,       // OS 동작 줄이기 1/0 (판정 불가 시 null). 자극에는 영향 없음
  "AUDIO_OK": null,          // 소리가 실제로 났는지 1/0. sound=0·장치 없음이면 null
  "SFX_COUNT": 0,            // 낸 신호 수(들렸는지와 무관). 게임 자극과의 청각 밀도 비교용
  "VOICE_OK": null,          // 나래이션이 실제로 났는지 1/0. sound=0·장치 없음이면 null (세탁 전용)
  "VOICE_SPOKEN": 0,         // 읽기를 요청한 문장 수(들렸는지와 무관) (세탁 전용)
  "scene_times": {"1":0.0, "...": 0.0},   // 장면별 체류(초)
  "scene_enter": {"1":0, "...": 0}        // 장면별 최초 진입 시각(epoch ms)
}
```

장면 10에서 나가는 길은 **둘**이다 — [지금 구매하기]와 오른쪽 위 [×]. 어느 쪽이든 눌림
피드백만 주고 외부 이동은 없다(모의 광고). 아무것도 안 누르면 3.07초 뒤 저절로 끝난다.

**왜 [×]가 있어야 하나.** 나가는 길이 구매뿐이면 그 클릭이 "사고 싶다"가 아니라 "나가려면
이것밖에 없다"가 된다. `CTA_CLICK`이 종속변인이라 이건 측정 문제고, 실제 광고에는 늘 있는
것이라 없으면 오히려 광고답지 않다. 게임 자극의 제품 카드에도 **같은 크기·같은 자리**의
같은 버튼이 있다(INTEGRATION.md §5-13) — 한쪽만 있으면 두 자극의 `CTA_CLICK` 비교가 깨진다.

세 갈래가 로그에서 갈린다: `CTA_CLICK=1` / `CLOSE_CLICK=1` / 둘 다 0(시간 경과).
`T_CARD`가 카드를 본 뒤 누르기까지의 시간이라, 바로 닫은 것과 끝까지 보고 닫은 것이 구분된다.

## 6. 개발 편의 (심사·리허설용)

- `?debug=1`: 우상단에 장면 점프 버튼, mode/ver 토글, 현재 로그 실시간 표시 패널.
- `?still=N`: 장면 N의 정지 화면으로 렌더(스토리보드 스크린샷 추출용 — IRB 별첨 제작에 사용).

## 7. 수용 기준 (9절 점검표 발췌 — 전 항목 통과해야 완료)

- [ ] 두 조건이 공유하는 장면(1·2·3·4·10)이 동일하다 (코드상 같은 함수 렌더)
- [ ] 실패 원인이 "시트 미사용" 하나로 읽힌다 (세탁기 고장·코스 실수 연출 없음)
- [ ] 되감기가 실패 결과를 충분히 보여준 뒤 일어나고, 되돌아간 지점에서 제품 적용이 가능하다
- [ ] 되감기를 참가자가 시작한다 ([되돌리기] 버튼, `T_REWIND` 기록, 게임 자극도 같은 자리)
- [ ] 수정 행동이 실제 사용법(시트를 세탁기에 넣기)과 일치하고 1단계 조작이다
- [ ] 행동 직후 광고가 즉각 반응한다 (드롭 → 애니메이션)
- [ ] 장면 7이 설명용 시각화임이 표시된다
- [ ] 장면 9 전후 비교에서 제품 사용 여부 외 조건이 동일하다 (같은 인물·셔츠·구도)
- [ ] "완전 방지"류 과장 문구가 없다
- [ ] A/B가 소재만 다르고 구조·길이·난이도·정보량이 같다
- [ ] 장면 10에서 나가는 길이 둘이다 ([×]가 있어 CTA 클릭이 강제가 아니다)
- [ ] 나래이션이 자막과 같은 말을 한다 (`npm test voice`)
- [ ] 연출이 조건 간 각성 비대칭을 만들지 않는다 (`npm test motion` 규칙 A~E)
- [ ] 오프라인·모바일 세로 전체화면에서 정상 동작, 로그 스키마 그대로 기록